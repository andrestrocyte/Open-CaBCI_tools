# Demo Outputs

These files were generated by:

```bash
python examples/run_core_demo.py
```

The PNG files are embedded in the main repository README. SVG versions are provided for manuscript or presentation editing. Numerical arrays, the fitted PCA model, significant network edges, and the compact run summary allow independent inspection without rerunning the demonstration.

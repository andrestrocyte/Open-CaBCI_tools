[pytest]
addopts = --strict-markers
testpaths = tests
markers =
    integration: runs the complete bundled-data demonstration
filterwarnings =
    ignore::DeprecationWarning:matplotlib\..*
    ignore::DeprecationWarning:skimage\..*
